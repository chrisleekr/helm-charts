# Chart-only process selection and network binding.
ROLE
PORT
# The triage worker's writable state directory under its /tmp emptyDir, set so
# a read-only root filesystem still has a HOME. An OS variable, not app config.
HOME

# Local development and isolated acceptance harness only.
ALLOW_LOCAL_PASSWORD_LOGIN
LOCAL_LOGIN_EMAIL
LOCAL_LOGIN_PASSWORD
ALLOW_SUPERUSER_APP_DB
ALLOW_INSECURE_APP_DB_PASSWORD
DEV_OBSERVABILITY_TUNNELS
DEV_KUBE_CONTEXT
DEV_PROMETHEUS_NAMESPACE
DEV_PROMETHEUS_SERVICE
DEV_PROMETHEUS_LOCAL_PORT
DEV_PROMETHEUS_REMOTE_PORT
DEV_GRAFANA_NAMESPACE
DEV_GRAFANA_SERVICE
DEV_GRAFANA_LOCAL_PORT
DEV_GRAFANA_REMOTE_PORT
SLACK_E2E_BOT_TOKEN
SLACK_E2E_CHANNEL
ALERTMANAGER_E2E_TIMEOUT_MS
SRE_DEV_CONNECTOR_LOOPBACK

# Build-time dashboard variables. Production injects the equivalent runtime keys.
VITE_AUTH0_DOMAIN
VITE_AUTH0_CLIENT_ID
VITE_AUTH0_AUDIENCE
VITE_API_BASE_URL
VITE_WS_BASE_URL

# An external embeddings endpoint selects its model server-side.
EMBEDDINGS_MODEL
